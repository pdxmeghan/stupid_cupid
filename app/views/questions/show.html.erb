<h1><%= @question.title %></h1>

<p><%= @question.content %></p>

<% if current_user && current_user.id == @question.user_id %>
  <%= link_to "Edit", question_path %> | <%= link_to "Delete", question_path %>
<% else %>
  <%= render 'answerform' %>
<% end %>
